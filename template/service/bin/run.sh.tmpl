#!/bin/bash

source "$(dirname "$0")"/utils.sh

if ! commandExist go;
then
  echo 'please install golang'
  exit 1
fi

project_dir="$(cd -- "$(dirname -- "$0")/.." &>/dev/null && pwd -P)"

cd $project_dir

go run -mod=mod cmd/{{.RepoName}}/main.go run --config config/config.yaml